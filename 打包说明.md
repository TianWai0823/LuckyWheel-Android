# 打包说明

## 方法一：使用 Android Studio 打包（推荐）

1. 在 Android Studio 中打开项目
2. 等待 Gradle 同步完成
3. 点击顶部菜单栏的 **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
4. 等待构建完成
5. 在右下角的通知中点击 "locate" 或在 `app/build/outputs/apk/debug/` 目录下找到 APK 文件

## 方法二：使用命令行打包

如果 Gradle 下载正常，可以执行：

```bash
.\gradlew.bat assembleDebug
```

APK 文件将生成在 `app\build\outputs\apk\debug\` 目录

## 常见问题

### Gradle 下载失败
- 检查网络连接
- 可以尝试使用 VPN
- 或者手动下载 Gradle 并放置到 `.gradle` 目录

### SSL 证书错误
- 将 `gradle\wrapper\gradle-wrapper.properties` 中的 `https` 改为 `http`
- 或者配置系统代理

## APK 文件位置

构建成功后，APK 文件位于：
```
D:\idea\work\MyFirstAndroid\app\build\outputs\apk\debug\app-debug.apk
```
